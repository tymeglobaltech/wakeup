services:
  wakeup-call:
    build: .
    container_name: wakeup-call
    ports:
      - "8080:3000"
    volumes:
      - ./data.json:/app/data.json
    restart: unless-stopped
    environment:
      - JWT_SECRET=${JWT_SECRET:-change-this-to-a-long-random-secret}
      - PORT=3000
